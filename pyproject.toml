[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "github-agent-bridge"
version = "1.2.0"
description = "GitHub transport and automation bridge for ChatGPT development and Codex local review."
readme = "README.md"
requires-python = ">=3.9"
license = {text = "MIT"}
authors = [{name = "Nagumo-Ryunosuke"}]
keywords = ["codex", "chatgpt", "agent", "bridge", "handoff", "github", "mcp", "automation"]
classifiers = [
  "Programming Language :: Python :: 3",
  "Programming Language :: Python :: 3.9",
  "License :: OSI Approved :: MIT License",
]
dependencies = []

[project.optional-dependencies]
mcp = ["mcp>=1.0"]

[project.urls]
Homepage = "https://github.com/Nagumo-Ryunosuke/github-agent-bridge"
Repository = "https://github.com/Nagumo-Ryunosuke/github-agent-bridge"

[project.scripts]
agent-bridge = "github_agent_bridge.cli:main"
agent-bridge-mcp = "github_agent_bridge.mcp_server:main"

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]
