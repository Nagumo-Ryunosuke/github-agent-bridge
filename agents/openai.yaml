interface:
  display_name: "GitHub Agent Bridge"
  short_description: "Codex asks; ordinary Chat designs, implements and reviews."
  default_prompt: "Use $github-agent-bridge with GPT-6 Astra as the Codex requirements dispatcher; relay design, implementation and review to ordinary browser Chat and verify delivery without creating Work tasks."
policy:
  allow_implicit_invocation: true
