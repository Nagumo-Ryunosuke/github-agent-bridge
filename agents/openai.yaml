display_name: GitHub Agent Bridge
short_description: Coordinate AI agents through commit-pinned GitHub handoffs.
default_prompt: Use GitHub Agent Bridge to inspect the repository's .ai state, detect the next agent action, validate commit context, and perform the appropriate task, implementation handoff, or review workflow.
